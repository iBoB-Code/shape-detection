cmake_minimum_required(VERSION 2.8)
project( shapeFinder )
find_package( OpenCV )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( shapeFinder shapeFinder.cpp )
target_link_libraries( shapeFinder ${OpenCV_LIBS} )
